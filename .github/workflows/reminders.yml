name: Daily deadline reminders

# Email reminders are disabled for now. Keep workflow_dispatch if we turn them on later.
on:
  workflow_dispatch:

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - name: Skip
        run: echo "Automatic email is disabled."
